<Window x:Class="NewStarTicket.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewStarTicket"
        xmlns:views="clr-namespace:NewStarTicket.Views"
        xmlns:viewModel="clr-namespace:NewStarTicket.ViewModels"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="StarTicket (Nouvelle édition !)" Height="675" Width="1200">

    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>

        <DataTemplate DataType="{x:Type viewModel:TicketListViewModel}">
            <views:TicketListView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:DashBoardViewModel}">
            <views:DashboardView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:StatisticViewModel}">
            <views:StatisticView/>
        </DataTemplate>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="125"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/Images/back-image1.png" Stretch="UniformToFill"/>
        </Grid.Background>

        <StackPanel Grid.Row="0"
                    Grid.Column="0">
            <Image Source="/Assets/Images/logo2.png" Width="125"/>
        </StackPanel>

        <!-- Message de bienvenu -->

        <TextBlock Text="{Binding CurrentUserAccount.DisplayName}"
                   FontSize="30"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Wheat"
                   Grid.Row="0"
                   Grid.Column="1"/>

        <TextBlock Text="{Binding Caption}"/>

        <!-- Barre de controle (la ou il y'a les boutons qui gerent l'affichage des menus -->

        <StackPanel Grid.Row="1"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    Background="{StaticResource color2}">
            <RadioButton Style="{StaticResource menuButton}" 
                         Tag="{StaticResource whiteColor}"
                         Command="{Binding ShowDashboardCommand}"
                         IsChecked="True">
                <TextBlock Text="Tabeau de bord" Style="{StaticResource menuButtonText}"/>
            </RadioButton>
            <RadioButton Style="{StaticResource menuButton}" 
                         Tag="{StaticResource whiteColor}"
                         Command="{Binding ShowTicketListCommand}">
                <TextBlock Text="Ticket" Style="{StaticResource menuButtonText}"/>
            </RadioButton>
            <RadioButton Style="{StaticResource menuButton}" 
                         Tag="{StaticResource whiteColor}"
                         Command="{Binding ShowStatisticCommand}">
                <TextBlock Text="Statistiques" Style="{StaticResource menuButtonText}"/>
            </RadioButton>
        </StackPanel>
        
        <!-- Partie contenu principal, avec la vue enfant -->

        <ContentControl Content="{Binding CurrentChildView}"
                        Grid.Row="2"
                        Grid.Column="1"
                        Margin="25"/>

    </Grid>
</Window>
